
# Upstream sources, cloned for reference only — never built from here.
/vendor/

# Build output.
/target/

# Byte-compiled sidecar sources; the .py files are the source of truth.
__pycache__/

# Screenshots taken while comparing the app against the design export.
/design/current/
packaging/uv
third-party/

# Signed runtime repository, built by scripts/tuf-repo.sh and published to the
# gh-pages branch. Serving state does not belong in the source history: it is
# rewritten in full on every publish and never read from here.
/dist/
/tuf/
# Belongs to the serving branch, which tuf-repo.sh writes for itself.
/.nojekyll
